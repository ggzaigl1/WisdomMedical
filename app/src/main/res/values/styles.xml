<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:listDivider">@drawable/shape_rv_divider</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@mipmap/icon_splash</item>
        <item name="android:windowFullscreen">true</item> <!-- 全屏 -->
        <item name="colorPrimary">@android:color/transparent</item>     <!-- ActionBar、Toolbar 的颜色 -->
        <item name="colorPrimaryDark">@android:color/transparent</item> <!-- 状态栏的颜色 -->
        <item name="colorAccent">@color/colorPrimaryDark</item>    <!-- EditText编辑时、FloatingActionButton背景等颜色 -->
        <item name="android:textAllCaps">false</item><!-- 所有字母是否大写 -->
        <item name="windowActionBar">false</item><!-- 不显示标题栏 -->
        <item name="windowNoTitle">true</item><!-- 无标题 -->
        <item name="android:colorBackgroundCacheHint">@null</item><!-- 背景缓存颜色 -->
    </style>


    <style name="TabLayoutTextStyle">
        <item name="android:textSize">@dimen/sp_16</item>
        <item name="android:textColor">@color/txtSuperColor</item>
    </style>

    <!--dialog总样式-->
    <style name="NiceDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowSoftInputMode">adjustPan</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <!--NiceDialog默认动画-->
    <style name="DefaultAnimation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/anim_slide_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/anim_slide_bottom_out</item>
    </style>

    <!-- 表示控件默认的颜色colorControlNormal
      colorControlActivated 表示控件被激活时的颜色 -->
    <style name="MyEditText" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@android:color/darker_gray</item>
        <item name="colorControlActivated">@color/lineColor</item>
    </style>


    <style name="CheckBoxStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:singleLine">true</item>
        <item name="android:textSize">@dimen/middle_font_size</item>
        <item name="android:textColor">@drawable/selector_tab_text_color</item>
        <item name="android:drawablePadding">4dp</item>
        <item name="android:drawableLeft">@drawable/selector_followup_radio</item>
        <item name="android:button">@null</item>
    </style>

</resources>
